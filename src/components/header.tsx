import * as React from 'react';

type Props = {
};
type State = {
};

export class Header extends React.Component<Props, State> {
  state = {};


  render() {
    return (
      <div>
        <div>Welcome to react to dad jokes</div>
      </div>
    );
  }
}

export default Header;
